package com.example.kotlinbasics

fun main () {

    // Step 3: Instantiating the Book Class

    var myBook = Book()

    // Step 4: Custom Values in Constructor

    var customBook = Book(author = "ABC", title = "DEF", yearPublished = 1998)

    // Review your code to ensure that it includes the following:

    //A Book class with a constructor and default values.

    //An instance of the Book class using default values.
    
    //An instance of the Book class using custom values.
}